#include "../include/block.h"
#include <iostream>

using namespace std;

//initializes starting values of block to dummy values
block :: block(int id = -1; const string& data = "", bool dummy = true){
    id = id;
    data = data;
    dummy = dummy;
};